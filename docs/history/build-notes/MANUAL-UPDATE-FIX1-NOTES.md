# SG-Panel UI23 Repair4 — Manual Update Fix 1

Основа: локальный Final Fix 2.

Изменён только запасной ручной способ обновления SG-Panel на странице Maintenance:

- удалена неопределённая фраза «распакуйте новый проверенный ZIP»;
- указан точный источник архива: GitHub `s-gor/sg-panel`, ветка `main`;
- добавлена полная последовательность команд скачивания, распаковки и запуска updater;
- распаковка выполняется через `python3 -m zipfile`, без обязательной зависимости от `unzip`;
- добавлено предупреждение, что этот способ ставит опубликованную GitHub-версию, а не локальную тестовую сборку.

Другие шаблоны, CSS, backend, Routing/WARP, SG-Node и GitHub не изменялись.
