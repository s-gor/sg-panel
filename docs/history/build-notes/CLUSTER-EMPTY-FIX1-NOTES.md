# SG-Panel UI23 Repair4 · Cluster Empty Fix 1

Локальная UI-правка пустого состояния Cluster поверх GeoFiles UI Fix 1.

Изменено только состояние без подключённых SG-Node:

- удалена большая внутренняя пустая карточка/строка;
- удалена дублирующая кнопка «+ Добавить SG-Node» внутри пустого состояния;
- сохранено компактное сообщение «SG-Node пока нет» с пояснением;
- внешний блок «Дополнительные серверы» и постоянно открытая форма добавления SG-Node сохранены;
- логика Cluster, подключение Node, backend и данные не менялись.

GitHub и SG-Node не затрагивались.
