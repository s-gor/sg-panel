SG-Panel Preview 9 · FIX40 · UI23 Repair4
Global Buttons Preview 3 Outline
Date: 2026-07-27

CURRENT CUMULATIVE LINE
- UI23 Repair4;
- Hysteria2 Salamander FinalMask;
- visible XMUX presets and corrected validation/apply controls;
- Xray Server radio cleanup;
- Routing Gateway Preview 1–3;
- Global Buttons Preview 1–3.

VISUAL CHANGE
- ordinary textual buttons are transparent with one shared outline;
- hover uses a calm translucent tint;
- selected/active controls receive the accent fill;
- a physical click temporarily uses the same accent fill;
- Routing unselected choices stay transparent;
- dark and light themes use the same state logic;
- inconsistent gold outlines on ordinary light-theme buttons are removed.

UNCHANGED
Backend, database, Xray candidate, Routing model, GeoFiles, Outbounds, WARP,
Cascade, XMUX, Salamander, backup and rollback.

LOCAL VERIFICATION
- pytest: 201 passed;
- Jinja templates: 38 parsed;
- Python compileall: passed;
- Bash syntax: passed;
- both .run --verify-only: passed;
- updater and full installer payloads match SOURCE byte-for-byte.

REQUIRED NEXT CHECK
Publish to GitHub main, wait for Tests and Verify current UI23 package, then
visually inspect dark and light themes before updating SG-Node.
