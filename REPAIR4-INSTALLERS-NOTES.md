# Repair4 updater and full installer

- `SG-PANEL-FIX40-UI23-REPAIR4-XMUX-UI-FIX1.run` updates an existing installation through `install-or-upgrade.sh`.
- `SG-PANEL-FIX40-FULL-UI23-REPAIR4-XMUX-UI-FIX1.run` performs a clean installation through `install.sh`.
- Both contain the same cumulative Repair4 XMUX UI Fix 1 source payload.
